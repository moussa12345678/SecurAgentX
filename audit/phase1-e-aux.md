# Phase 1-E — Auxiliary Directory Scan

**Task ID:** P1-E
**Scope:** All auxiliary top-level directories under `/home/z/my-project/securagentx-work/` (excluding `elengenix/`, `tests/`, `.git/`, `.github/`).
**Method:** `find <dir> -type f -name "<ext>"` for counts; `grep -rIl -i elengenix <dir>` for file counts; `grep -rIoh -i elengenix <dir> | wc -l` for total occurrences.

## 1. Per-Aux-Dir Summary

| Dir | `.py` | `.md` | `.json` | `.yaml`/`.yml` | total files | files w/ `elengenix` | `elengenix` occurrences |
|---|---:|---:|---:|---:|---:|---:|---:|
| agents          |  31 | 0 | 0 | 0 |  31 | 28 |  76 |
| assets          |   0 | 0 | 0 | 0 |   7 |  3 |   4 |
| cli             |   8 | 0 | 0 | 0 |  14 | 12 |  57 |
| commands        |   6 | 0 | 0 | 0 |  12 | 12 |  68 |
| core            |   5 | 0 | 0 | 0 |  10 |  6 |  56 |
| docs            |   0 | 3 | 0 | 0 |   3 |  3 |  25 |
| examples        |   2 | 1 | 0 | 2 |   5 |  5 |  15 |
| integrations    |   3 | 0 | 0 | 0 |   3 |  2 |  10 |
| knowledge       |   1 | 1 | 0 | 0 |   2 |  2 |   2 |
| mcp             |   6 | 0 | 0 | 0 |  12 | 12 |  58 |
| pipeline        |   1 | 0 | 0 | 0 |   1 |  1 |   4 |
| prompts         |   1 | 0 | 0 | 1 |   5 |  2 |   6 |
| redteam_agent   |   1 | 0 | 0 | 0 |   1 |  0 |   0 |
| scripts         |   4 | 0 | 0 | 0 |   4 |  4 |   8 |
| tools           | 140 | 1 | 0 | 0 | 141 | 136 | 427 |
| tui             |  11 | 0 | 0 | 0 |  11 |  9 |  45 |
| **TOTALS**      | **220** | **6** | **0** | **3** | **262** | **237** | **861** |

### Notes on `total files` vs extension columns
- **`*,cover` artifacts:** Four dirs (`cli`, `commands`, `core`, `mcp`) each contain pytest coverage data files named `*.py,cover`. These are **23** binary-ish text artifacts (regenerable), not source files. They are counted in `total files` but **not** in the `.py` column (extension is `.py,cover`, not `.py`). They do contain `elengenix` strings, so they inflate the `files w/ elengenix` count for those 4 dirs.
  - cli: 6 cover files; commands: 6; core: 5; mcp: 6.
- **assets/** contains 7 binary/image files (2 PNG, 1 JPG, 4 SVG). The 3 files matching `elengenix` are `elengenix.png`, `elengenix-red.png`, and `logo-animated.svg` (which embeds the brand text).
- **prompts/** has 5 files: `__init__.py`, `agent_prompt.txt`, `system_prompt.txt`, `vuln_finder_system.txt`, and `few_shots/sqli.yaml`. The two `.txt` prompt files contain `elengenix` brand mentions; only the `.yaml` is counted under the yaml column.
- **examples/** has 5 files across `examples/plugins/{hello_world,ollama_local}/` (2× `__init__.py`, 2× `plugin.yaml`, 1× `README.md`).
- **redteam_agent/** has a single `__init__.py` (empty/skeleton) with **zero** `elengenix` occurrences — the cleanest aux dir.

## 2. Top-Level `main.py`

| Metric | Value |
|---|---:|
| Total lines | **2,818** |
| `elengenix` occurrences (case-insensitive) | **91** |

This is the densest single source file for `elengenix` brand references in the aux layer.

## 3. Top-Level `__init__.py`

| Metric | Value |
|---|---:|
| Total lines | 1 |
| `elengenix` occurrences | **1** |

Contents (single line):
```python
"""Elengenix - Autonomous AI Agent Framework for Security Research."""
```

## 4. Other Top-Level Files of Interest

| File | Type | Size | `elengenix` occurrences | Notes |
|---|---|---:|---:|---|
| `apply_to_fork.sh` | Bash (UTF-8, executable) | 5,059 B | 23 | Fork-patch apply script |
| `apply_to_fork_termux.sh` | Termux bash (UTF-8, executable) | 3,762 B | 7 | Termux variant of the above |
| `elengenix-pentagi-integration.tar.gz` | **gzip binary tarball** | 775,547 B | n/a (binary) | ⚠️ DO NOT rename / modify |

### ⚠️ Tarball integrity note

`elengenix-pentagi-integration.tar.gz` is a **binary artifact** (gzip compressed, original size ~3.5 MB). It is a packaged integration snapshot and **must NOT be renamed, re-compressed, or modified in any way** during the renaming audit. Per task instructions: leave the filename and bytes untouched.

For integrity tracking (reference only — no action taken):
- MD5: `de6bb4d88cfb7f131cd018547b1e5cd5`
- Size: 775,547 bytes

If the rename procedure later needs to expose the integration under the new brand name, the correct approach is to extract the tarball into a working copy, rename *extracted* contents, and produce a **new** tarball under the new name — leaving this original file in place as an immutable source of truth.

## 5. Observations & Risk Flags for Later Phases

1. **High-density dirs (rename effort hotspots):** `tools/` (427 occurrences across 136 files), `agents/` (76 / 28), `commands/` (68 / 12), `mcp/` (58 / 12), `cli/` (57 / 12), `core/` (56 / 6), `tui/` (45 / 9). These will absorb the bulk of the rename effort.
2. **Coverage artifacts:** The 23 `*,cover` files in `cli/commands/core/mcp` are auto-generated pytest coverage data and will be regenerated by the test suite; they should be **excluded** from the rename pass (or deleted and regenerated post-rename).
3. **Binary brand assets:** `assets/elengenix.png` and `assets/elengenix-red.png` are raster images — their filename contains the brand name but the embedded pixels may also show the logo. Renaming the file is safe; regenerating the logo image is out of scope for a text-rename audit.
4. **`redteam_agent/`** is empty-of-brand (`0` occurrences) — only an `__init__.py` skeleton. No action needed there.
5. **`main.py` (91 occurrences)** is the single largest textual hot-spot outside `tools/`. Recommend a dedicated pass for that file.
6. **Apply scripts** (`apply_to_fork*.sh`) — 30 occurrences combined. These scripts likely reference the `elengenix` upstream URL/branch; renaming must preserve git remote semantics. Flag for careful review, not blind sed.

## 6. Excluded Directories (per task spec)

- `elengenix/` — the package being renamed (covered by other phases).
- `tests/` — covered by other phases.
- `.git/`, `.github/` — version-control metadata, out of scope.
- `audit/` — this output directory (created empty; now contains this report).
